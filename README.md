# NJA 2026 landing page

A minimal landing page built with Astro.

## Local development

```sh
npm install
npm run dev
```

Open `http://localhost:4321` in your browser.

## Production build

```sh
npm run build
```

The static site is generated in `dist/`. Vercel detects Astro automatically, so no additional deployment configuration is needed for this starter.
